# Graphify — qué NO entra al grafo (misma sintaxis que .gitignore; se suma a él).
node_modules/
.next/
graphify-out/
*.tsbuildinfo
package-lock.json
.pgdata-test/

# AIOS Constelarys vive acá solo como carpeta de trabajo; tiene su propio grafo
Level 2.0/

# binarios
*.png
*.jpg
*.jpeg
*.gif
*.webp
*.pdf
*.mp4
*.zip
Images for the web/
public/

# copias de skills de terceros (las MISMAS skills triplicadas por IDE): ruido puro,
# ~100 archivos que no son conocimiento de este proyecto
.agents/
.claude/
.windsurf/
Changes/

# Lo archivado NO entra al grafo: son docs que DEJARON de ser verdad y el grafo
# no los distingue de los vivos — una consulta los devuelve mezclados con el codigo
# de hoy. El historial sigue en git y en docs/archive/ para leerlo a proposito.
docs/archive/
docs/AUDIT-12-Julio/
